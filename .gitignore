# Dependencies — never commit these (large, rebuildable with `npm install`)
node_modules/

# Secrets & credentials — keep these OUT of the repo
.env
.env.*
*.pem
*.key
secrets.json
credentials.json

# Server log files (created when running the app)
server.out
server.err

# Logs & OS / editor cruft
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.vscode/
.idea/
